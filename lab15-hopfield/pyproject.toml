[project]
name = "lab15-hopfield"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kamil Kania", email = "kkania388@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.3",
    "tabulate>=0.10.0",
]

[project.scripts]
lab15-hopfield = "lab15_hopfield:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
